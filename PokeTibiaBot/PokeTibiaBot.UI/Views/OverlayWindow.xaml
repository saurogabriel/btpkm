<Window x:Class="PokeTibiaBot.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Width="260" Height="140"
        Left="40" Top="40"
        Title="Overlay"
        FontFamily="Consolas">
    <Border CornerRadius="10"
            Background="#CC0F1419"
            BorderBrush="#F0B429"
            BorderThickness="1.5"
            Padding="12">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="◉ POKETIBIA BOT" FontWeight="Bold"
                           Foreground="#F0B429" FontSize="12"/>
                <TextBlock Grid.Column="1" x:Name="StateText"
                           Text="Idle" FontWeight="Bold"
                           Foreground="#8B949E" FontSize="11"/>
            </Grid>

            <TextBlock Text="HP" Foreground="#E8ECEF" FontSize="10" Margin="0,6,0,2"/>
            <Grid Height="14">
                <Border Background="#1A2029" CornerRadius="3"/>
                <Border x:Name="HpBar" HorizontalAlignment="Left"
                        Background="#E53935" CornerRadius="3" Width="200"/>
                <TextBlock x:Name="HpText" Text="100%"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="White" FontWeight="Bold" FontSize="10"/>
            </Grid>

            <TextBlock Text="MP" Foreground="#E8ECEF" FontSize="10" Margin="0,4,0,2"/>
            <Grid Height="14">
                <Border Background="#1A2029" CornerRadius="3"/>
                <Border x:Name="MpBar" HorizontalAlignment="Left"
                        Background="#5B9BD5" CornerRadius="3" Width="200"/>
                <TextBlock x:Name="MpText" Text="100%"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="White" FontWeight="Bold" FontSize="10"/>
            </Grid>

            <TextBlock Text="WAYPOINT" Foreground="#8B949E" FontSize="9" Margin="0,8,0,2"/>
            <TextBlock x:Name="WpText" Text="—"
                       Foreground="#43A047" FontWeight="Bold" FontSize="11"
                       TextTrimming="CharacterEllipsis"/>
        </StackPanel>
    </Border>
</Window>
